import {DB_Type} from "./Types";

export const db:DB_Type = {
    existingBlogs : [{
        "id": "string",
        "name": "string",
        "description": "string",
        "websiteUrl": "string"
    }],
    existingPosts : []
}